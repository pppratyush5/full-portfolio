const ErrorScreen = () => {
  return (
    <div
      style={{
        textAlign: "center",
        position: "absolute",
        top: "50%",
        left: "50%",
        transform: "translate(-50%,-50%)",
      }}
    >
      error...! page not found
    </div>
  );
};

export default ErrorScreen;
